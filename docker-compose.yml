version: "3.9"

services:
    ds-bot:
        build: .
        container_name: ds-bot
        restart: always
        volumes:
            - ./db.db:/opt/app/db.db
        env_file:
            - .env
        environment:
            - NODE_ENV=production
        command: >
            sh -c "
                npm run migrate:prod
                npm run update-slash-commands:prod
                npm run prod
            " 
